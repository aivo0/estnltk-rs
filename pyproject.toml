[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "estnltk-regex-rs"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
manifest-path = "estnltk-python/Cargo.toml"
features = ["pyo3/extension-module", "vabamorf"]
